# Artefak build & dependency — di-install/di-build ulang di dalam image
node_modules
.next
out
dist
build

# VCS, tooling repo, CI — tidak dibutuhkan di image
.git
.github
.husky
.vscode
Jenkinsfile
docs

# Semua env file (termasuk .env.prod dan secrets) tidak boleh masuk build context.
.env*

# Lain-lain
.DS_Store
*.log
Dockerfile
docker-compose.yml
.dockerignore
